name: Deploy to Railway

on:
  push:
    branches:
      - main

env:
  RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
  RAILWAY_PROJECT_ID: ${{ vars.RAILWAY_PROJECT_ID }}
  RAILWAY_ENVIRONMENT_ID: ${{ vars.RAILWAY_ENVIRONMENT_ID }}

jobs:
  deploy-back:
    name: Deploy Backend
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy backend service
        run: railway up --ci -m "Deploy ${{ github.sha }} — ${{ github.event.head_commit.message }}"
        working-directory: back
        env:
          RAILWAY_SERVICE_ID: ${{ vars.RAILWAY_SERVICE_BACK_ID }}

  deploy-worker:
    name: Deploy Worker
    runs-on: ubuntu-latest
    environment: production
    needs: deploy-back
    steps:
      - uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy worker service
        run: railway up --ci -m "Deploy ${{ github.sha }} — ${{ github.event.head_commit.message }}"
        working-directory: back
        env:
          RAILWAY_SERVICE_ID: ${{ vars.RAILWAY_SERVICE_WORKER_ID }}

  deploy-front:
    name: Deploy Frontend
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy frontend service
        run: railway up --ci -m "Deploy ${{ github.sha }} — ${{ github.event.head_commit.message }}"
        working-directory: front
        env:
          RAILWAY_SERVICE_ID: ${{ vars.RAILWAY_SERVICE_FRONT_ID }}
